# ROADMAP — развитие функционала бота

Рабочий план по фазам. Двигаемся последовательно, каждая фаза — отдельная сессия разработки. Не начинать следующую фазу, пока предыдущая не закрыта и не проверена руками.

**Принцип, зафиксированный по итогам обсуждения:** бот фиксирует значения (еда, вес, шаги, вода). Он не советует, не корректирует цели сам и не различает дни по типу активности. Любая функция, которая начинает «решать за пользователя» или «учить» — вне периметра этого проекта на данном этапе.

---

## Вне периметра (сознательно не делаем)

| Идея | Почему нет |
|---|---|
| Разные таргеты КБЖУ на тренировочные/дни отдыха | Это уже логика коуча, а не трекера. Пользователь сам знает свой день — бот просто пишет цифры |
| Авто-детектор плато / авто-предложение поднять калораж | Тоже коучинг. Решение о смене таргета — целиком на пользователе, через `/reset` или будущий `/profile edit` |
| Персонализация расписания scheduler.js под график каждого юзера (сейчас жёстко MSK, один сценарий на всех) | Низкий приоритет, не связано с фиксацией данных. Отложено — вернёмся, если станет реальной болью на нескольких пользователях |
| Полноценный трекер тренировок (подходы/веса/прогрессия) | Отдельный домен, не относится к «food tracker» |

---

## Фаза 1 — Редактируемый журнал веса

**Проблема:** `users.weight_kg` пишется один раз при онбординге и потом не обновляется никогда (кроме полного `/reset`, который стирает весь профиль).

**Решение:**
- Новая таблица `weight_logs` (`telegram_id`, `log_date`, `weight_kg`, `logged_at`) — по образцу `water_logs`.
- Команда `/вес 89.4` (алиас `/weight`) — пишет запись на сегодня. Если на сегодня уже есть запись — перезаписывает (а не дублирует).
- `/вес` без числа — показывает последнюю записанную цифру и дату.
- При каждой записи — синхронно обновлять `users.weight_kg` (последнее известное значение), чтобы `/profile` показывал актуальный вес без доп. JOIN.
- Миграция: `migration_weight_log.sql`.

**Acceptance:**
- Можно записать вес в любой день, поверх старого значения того же дня.
- `/profile` показывает вес из последней записи, а не из онбординга.
- История сохраняется по датам (нужна для Фазы 4 — выгрузки).
- Тот же функционал доступен в Mini App: карточка «Вес» рядом с водой — редактируемая для сегодня, read-only при просмотре прошлых дней (или «не записан», если записи не было).

---

## Фаза 2 — Шаги (ручной ввод)

**Решение:**
- Новая таблица `steps_logs` (`telegram_id`, `log_date`, `steps`) — одно значение в день, перезаписывается при повторном вводе (не суммируется, в отличие от воды).
- Команда `/шаги 8500` (алиас `/steps`) — записывает/обновляет шаги за сегодня.
- `/шаги` без числа — показывает сегодняшнее значение.
- Миграция: `migration_steps_log.sql` (можно объединить с Фазой 1 в один файл `migration_weight_steps.sql` — решим при реализации).

**Acceptance:**
- Значение шагов за день можно вводить и перезаписывать вручную.
- Данные доступны для выгрузки (Фаза 4).

---

## Фаза 3 — Фикс: смена цели не должна переписывать историю

**Проблема:** `buildTotalsText()` (`src/bot/handlers/food.js:69-135`) и `eveningSummary()` (`src/services/scheduler.js:134-196`) на каждый запрос берут `daily_calories/protein/fat/carbs` из ТЕКУЩЕЙ строки `users`. Если пользователь позже меняет цель (`/reset` или будущий `/profile edit`), все прошлые дни в Mini App и в отчётах молча пересчитываются под новую цель — история искажается задним числом.

**Решение:**
- При первом логе за день — фиксировать («снапшотить») текущий таргет пользователя в `food_logs` (4 новых nullable-колонки: `target_calories`, `target_protein_g`, `target_fat_g`, `target_carbs_g`) или в отдельную таблицу `daily_targets(telegram_id, log_date, calories, protein_g, fat_g, carbs_g)` — определимся при реализации, вероятно вариант с отдельной таблицей чище.
- Текущий день (сегодня) может по-прежнему сверяться с живым профилем (цель ещё не «закрыта»), но как только день закончился — таргет для него фиксирован навсегда.
- Обновить `buildTotalsText`, `eveningSummary`, Mini App историю (`src/webapp/app.js`) — читать таргет прошлых дней из снапшота, а не из `users`.
- Миграция: `migration_daily_targets.sql`.

**Acceptance:**
- Меняем цель в профиле → открываем прошлый день в Mini App → проценты/остатки считаются от таргета, который действовал в ТОТ день, а не от нового.

---

## Фаза 4 — Еженедельная выгрузка статистики

**Решение:**
- Новая команда `/неделя` (алиас `/week`, `/export`) — собирает данные за последние 7 календарных дней (MSK) и формирует `.md`-файл, который бот отправляет как документ (`ctx.replyWithDocument`).

**Содержимое файла:**
- Таблица по дням: дата · калории (факт/цель) · Б/Ж/У (факт/цель) · вес (если есть запись) · шаги (если есть запись)
- Итоги: среднее по калориям/макросам за неделю, изменение веса за период (последняя запись минус первая в диапазоне, если обе есть)
- Если данных за день нет — явно писать «нет данных», не подставлять нули молча (тот же принцип, что уже используется в `eveningSummary` для нулевого дня)

**Acceptance:**
- `/неделя` работает даже если веса/шагов вообще ни разу не вводили (просто не показывает эти колонки/пишет «нет данных»).
- Файл читается как обычный текст — без HTML-тегов, без Markdown-мусора, который не рендерится вне Telegram.

---

## Фаза 5 (опционально, отдельно подтвердить перед стартом) — meal_type по времени лога

**Проблема:** поле `food_logs.meal_type` существует в схеме, но `insertLog()` (`src/bot/handlers/food.js:39-67`) никогда его не передаёт — всегда падает в дефолт `'snack'`.

**Решение:** при записи лога определять `breakfast/lunch/dinner/snack` по часу лога (MSK), без участия ИИ и без вопроса пользователю. Просто корректная категоризация уже вводимых данных — не новая функция, а починка неиспользуемого поля.

**Зачем помечено отдельно:** не обсуждали явно в переписке — прежде чем брать в работу, подтвердить, что это нужно (например, для будущей разбивки по приёмам в выгрузке или Mini App).

---

## Статус

| Фаза | Статус |
|---|---|
| 1 — Вес | ✅ Готово (бот-команда `/вес` + карточка в Mini App), нужен прогон миграции на реальной БД и ручная проверка обоих путей |
| 2 — Шаги | Не начато |
| 3 — Фикс истории таргета | Не начато |
| 4 — Еженедельная выгрузка | Не начато |
| 5 — meal_type (опционально) | Ждёт подтверждения |
